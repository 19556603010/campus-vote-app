# 校园投票小程序 - 第三次课升级方案

> 适用范围:`campus-vote-app`(当前已具备基础云开发架构)
> APPID:`wxe21b774f157ccea6`
> 云环境ID:`cloud1-d4g13ncrb52e37834`
> 参考规范:[微信云开发入门](https://developers.weixin.qq.com/miniprogram/dev/wxcloudservice/wxcloud/basis/getting-started.html)

---

## 一、升级目标

在已有的"云开发基础架构(7个云函数 + 2个集合)"上,**第三次课**聚焦于**互动层与用户层**的增强:

| 模块 | 已有 | 新增 |
|------|------|------|
| 投票CRUD | ✅ | — |
| 投票记录(防重) | ✅ | — |
| **评论与讨论系统** | ❌ | ✅ 本次核心 |
| **用户中心(降级方案)** | ❌ | ✅ 配套 |
| **底部Tab变3个** | ❌ | ✅ 配套 |
| 列表显示评论数 | ❌ | ✅ 增强 |

> 用户系统采用**降级方案**:启动时随机分配昵称,用户可在"我的"页面主动编辑;**不主动调用** `wx.getUserProfile`,避免触发授权弹窗影响体验。

---

## 二、最终文件改动清单

### 2.1 新增文件

| 路径 | 用途 |
|------|------|
| `miniprogram/cloudfunctions/addComment/index.js` | 提交评论 |
| `miniprogram/cloudfunctions/addComment/package.json` | 依赖声明 |
| `miniprogram/cloudfunctions/getComments/index.js` | 拉取评论列表(分页) |
| `miniprogram/cloudfunctions/getComments/package.json` | 依赖声明 |
| `miniprogram/cloudfunctions/deleteComment/index.js` | 删除自己的评论 |
| `miniprogram/cloudfunctions/deleteComment/package.json` | 依赖声明 |
| `miniprogram/cloudfunctions/getMyCreatedVotes/index.js` | "我的"页 - 我创建的 |
| `miniprogram/cloudfunctions/getMyCreatedVotes/package.json` | 依赖声明 |
| `miniprogram/cloudfunctions/getMyVotedVotes/index.js` | "我的"页 - 我参与的 |
| `miniprogram/cloudfunctions/getMyVotedVotes/package.json` | 依赖声明 |
| `miniprogram/cloudfunctions/updateUserProfile/index.js` | 更新昵称/头像(降级方案) |
| `miniprogram/cloudfunctions/updateUserProfile/package.json` | 依赖声明 |
| `miniprogram/pages/mine/mine.js` | 个人主页(第3个Tab) |
| `miniprogram/pages/mine/mine.json` | 页面声明 |
| `miniprogram/pages/mine/mine.wxml` | 个人主页结构 |
| `miniprogram/pages/mine/mine.wxss` | 个人主页样式 |

### 2.2 修改文件

| 路径 | 改动点 |
|------|--------|
| `miniprogram/app.js` | ① 替换 `env` 占位符 ② 增加 `globalData.userInfo` ③ 启动时从本地存储恢复昵称 ④ 暴露 `updateUserInfo()` 方法 |
| `miniprogram/app.json` | ① 注册新页面 `pages/mine/mine` ② tabBar 新增第3项"我的" |
| `miniprogram/cloudfunctions/getVotes/index.js` | 聚合查询 `comments` 集合,给每条 vote 加 `commentCount` |
| `miniprogram/cloudfunctions/deleteVote/index.js` | 级联删除 `comments` 中 `voteId` 匹配的所有评论 |
| `miniprogram/pages/index/index.wxml` | 投票卡片显示评论数 |
| `miniprogram/pages/detail/detail.wxml` | 投票详情底部增加评论区 |
| `miniprogram/pages/detail/detail.js` | 加载/提交/删除评论 |
| `miniprogram/pages/detail/detail.wxss` | 评论样式 |
| `miniprogram/pages/create/create.js` | 改用 `app.globalData.userInfo.nickName` 替代伪随机昵称 |

---

## 三、数据库集合(本次共 3 个)

### 3.1 已有集合(保持不变)

#### 集合 A:`votes`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `_id` | string | 系统 | 文档ID |
| `_openid` | string | 系统 | 创建者openid(系统自动) |
| title | string | ✅ | 投票标题 |
| description | string |   | 描述 |
| options | array | ✅ | `[{text:string, votes:number}]` |
| voteType | string | ✅ | `single` / `multiple` |
| endDate | string\|null |   | 截止日期 |
| isAnonymous | boolean |   | 是否匿名 |
| creatorName | string |   | 创建者昵称(降级方案:取 globalData) |
| creatorOpenId | string |   | 创建者openid(冗余,便于查询) |
| createTime | date | ✅ | 服务端时间 |
| totalVoters | number |   | 参与人数 |
| status | string |   | `active` / `ended` |
| **commentCount** | number |   | **🆕 评论数(冗余字段,由 getVotes / submitVote 维护)** |

**权限(JSON 写入"权限设置"):**

```json
{
  "read": true,
  "write": "doc._openid == auth.openid"
}
```

**索引(在"索引管理"添加):**

| 索引名 | 字段 | 排序 | 用途 |
|--------|------|------|------|
| `idx_createTime` | `createTime` | desc | 列表按时间倒序 |

#### 集合 B:`vote_records`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `_id` | string | 系统 | 文档ID |
| `_openid` | string | 系统 | 系统自动注入 |
| voteId | string | ✅ | 关联 votes._id |
| userOpenId | string | ✅ | 用户openid(冗余) |
| selectedOptions | array | ✅ | 选项索引列表 |
| voteTime | date | ✅ | 服务端时间 |

**权限:**

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**索引:**

| 索引名 | 字段 | 排序 | 用途 |
|--------|------|------|------|
| `idx_vote_user` | `voteId, userOpenId` | asc, asc | 查询"我是否投过" |
| `idx_user_time` | `userOpenId, voteTime` | asc, desc | "我参与的"列表 |

---

### 3.2 🆕 新增集合

#### 集合 C:`comments` (本次新增)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `_id` | string | 系统 | 文档ID |
| `_openid` | string | 系统 | 评论者openid(系统自动) |
| voteId | string | ✅ | 关联 votes._id |
| userOpenId | string | ✅ | 评论者openid(冗余) |
| userNickName | string | ✅ | 评论时昵称(快照,防改名) |
| userAvatar | string |   | 评论时头像(快照) |
| content | string | ✅ | 评论内容(1~500字) |
| parentId | string |   | 父评论ID(预留,本次先不做回复) |
| likeCount | number |   | 点赞数(本次先恒为0) |
| status | string |   | `normal` / `hidden`(本次恒为 normal) |
| createTime | date | ✅ | 服务端时间 |

**权限(JSON 写入"权限设置"):**

```json
{
  "read": true,
  "write": "doc._openid == auth.openid"
}
```

> 说明:write 限制为"本人"即可,实际删除操作在云函数中二次校验 `_openid`,因为安全规则只能写不能单独配置"仅可创建不可改"。

**索引(在"索引管理"添加):**

| 索引名 | 字段 | 排序 | 用途 |
|--------|------|------|------|
| `idx_vote_time` | `voteId, createTime` | asc, desc | 按投票ID分页拉评论 |
| `idx_user` | `userOpenId` | asc | 预留:个人中心"我的评论" |

---

## 四、云函数清单(本次共 13 个)

### 4.1 已有(7个,改动标注)

| 云函数 | 改动 |
|--------|------|
| `getOpenId` | 无 |
| `getVotes` | **🆕 聚合查询** `comments`,给 vote 加 `commentCount` 字段 |
| `getVoteDetail` | 无(详情页里另调 `getComments` 拉评论) |
| `createVote` | 无 |
| `submitVote` | 无 |
| `deleteVote` | **🆕 级联删除** `comments` 中 `voteId` 匹配的评论 |
| `checkVoteRecord` | 无 |

### 4.2 🆕 新增云函数(6个)

| 云函数 | 入参 | 返回 | 说明 |
|--------|------|------|------|
| `addComment` | `{ voteId, content }` | `{ success, _id, commentCount }` | 提交评论;服务端 `+1` `votes.commentCount` |
| `getComments` | `{ voteId, page, pageSize }` | `{ success, list, total, hasMore }` | 分页拉评论,按时间倒序 |
| `deleteComment` | `{ commentId }` | `{ success, commentCount }` | 仅 `_openid == auth.openid` 可删;同步 `-1` |
| `getMyCreatedVotes` | `{ page, pageSize }` | `{ success, list, total, hasMore }` | `_openid == OPENID` 的 votes |
| `getMyVotedVotes` | `{ page, pageSize }` | `{ success, list, total, hasMore }` | 通过 `vote_records` 聚合出参与过的 votes |
| `updateUserProfile` | `{ nickName, avatarUrl? }` | `{ success, userInfo }` | 降级方案:把昵称/头像存到 `users` 集合(`_id == OPENID`) |

> `updateUserProfile` 需要新增集合 `users`,**权限严格**;如果觉得复杂度高,可以在 `app.js` 内用 `wx.setStorageSync` 完成,云函数可省略(本方案**默认省略** `users` 集合与该云函数,昵称走本地存储即可)。

---

## 五、UI 改造要点

### 5.1 `app.json`

```json
{
  "pages": [
    "pages/index/index",
    "pages/create/create",
    "pages/mine/mine",
    "pages/detail/detail"
  ],
  "tabBar": {
    "list": [
      { "pagePath": "pages/index/index",  "text": "投票" },
      { "pagePath": "pages/create/create", "text": "发起" },
      { "pagePath": "pages/mine/mine",     "text": "我的" }
    ]
  }
}
```

### 5.2 `pages/mine/mine` 个人主页

页面分两段:
- **顶部资料卡**:头像、昵称(可点"编辑")、openid(后几位)
- **Tabs**:`我发起的` / `我参与的`
  - "我发起的" 调 `getMyCreatedVotes`
  - "我参与的" 调 `getMyVotedVotes`

### 5.3 `pages/detail/detail` 评论区

在原详情页底部(分享按钮下)新增:

- 评论列表(头像 + 昵称 + 时间 + 内容)
- 自己评论旁有"删除"按钮
- 底部固定输入栏(头像 + input + "发送")
- 输入框字数限制 500

### 5.4 `pages/index/index` 评论数

投票卡片底部加:`💬 {{item.commentCount || 0}}` 图标 + 数字。

---

## 六、你的执行步骤(分阶段引导)

### 阶段 1:开通云开发并替换环境ID

1. 打开**微信开发者工具**,导入项目 `campus-vote-app`
2. 顶部菜单「云开发」→「开通」
3. 选择**免费配额**,创建云环境
4. **环境名称** 任意(例如 `campus-vote`)
5. **环境ID** 取回(若与 `cloud1-d4g13ncrb52e37834` 不一致,告诉我)

> ⚠️ 你给的 `cloud1-d4g13ncrb52e37834` 是目标环境ID,我会写入 `app.js` 第7行。

### 阶段 2:创建数据库集合

在 [微信云开发控制台](https://cloud.weixin.qq.com/) →「数据库」,按以下顺序操作:

#### 第 1 步:创建 `votes`(已存在则跳过)

若项目**第一次部署**,按上一份升级指南创建;**权限**与**索引**保持上方**集合 A** 的说明。

#### 第 2 步:创建 `vote_records`(已存在则跳过)

同上,对应**集合 B**。

#### 第 3 步:🆕 创建 `comments`

1. 点「+ 添加集合」→ 名称 `comments`
2. 权限设置粘贴:
   ```json
   { "read": true, "write": "doc._openid == auth.openid" }
   ```
3. 进 `comments` →「索引管理」→「+ 添加索引」:
   - 索引名 `idx_vote_time`,字段 `voteId` asc + `createTime` desc
4. 再加一个:`idx_user`,字段 `userOpenId` asc

> 💡 **重要**:添加索引时,字段类型全部选 `string`(除 createTime 选 `date`),方向按上表。

### 阶段 3:部署云函数

#### 3.1 部署已有的 7 个

微信开发者工具中,右键 `miniprogram/cloudfunctions/getOpenId` →「上传并部署:云端安装依赖」。**7个全部**逐个部署。

#### 3.2 部署新增的 5 个

按以下顺序部署(右键文件夹 →「上传并部署:云端安装依赖」):

1. `addComment`
2. `getComments`
3. `deleteComment`
4. `getMyCreatedVotes`
5. `getMyVotedVotes`

> 每个云函数首次部署会自动 `npm install wx-server-sdk`,等待 30s~1min。

### 阶段 4:我帮你写代码

代码已规划完毕,我会:
1. 替换 `app.js` 的 `env` 占位符 → `cloud1-d4g13ncrb52e37834`
2. 修改 `app.json` 注册 mine 页面 + 第3个 tabBar
3. 修改 `getVotes` / `deleteVote` 云函数
4. 新增 5 个云函数
5. 新增 `pages/mine/`
6. 修改 `index` / `detail` / `create` 页面

### 阶段 5:联调测试

1. 点击「编译」,小程序应能正常进入
2. 测试"创建投票 → 详情页评论 → 列表页看评论数"
3. 切到「我的」tab,查看"我发起的 / 我参与的"

---

## 七、安全 & 边界说明

- 评论字数:前端限制 500,云函数 `addComment` 再次校验
- 评论内容:不在客户端做 XSS 转义(微信 WXML 默认安全),但服务端要 `trim` 并拒空
- 删除评论:云函数二次校验 `_openid == auth.openid`
- 用户昵称降级:仅本地存储,云函数从 `OPENID` 反推,无需新建 `users` 集合
- 投票的 `commentCount`:在 `addComment` / `deleteComment` / `deleteVote` 中**事务**式更新

---

## 八、提交给用户的确认问题

- [ ] **Q1**:`cloud1-d4g13ncrb52e37834` 这个环境ID是否就是你的真实环境?如果不是,告诉我新的。
- [ ] **Q2**:`comments` 集合名是否冲突?(若已存在同名集合,先告诉我,避免覆盖)
- [ ] **Q3**:`getMyVotedVotes` 是否要做分页?(若数据少,可以一次返回)
- [ ] **Q4**:TabBar 第3个图标用 emoji 兜底,还是需要下载 .png 放进 `images/tab/`?(本方案用 emoji `👤`,**不创建图片文件**)

**确认无误后,我会开始编码。**
