# 校园投票小程序 - 第三次课升级指南

## 项目已完成的升级

### 1. 云开发架构改造

原项目使用本地 Storage 存储数据，已升级为云开发架构：

**改造内容：**
- ✅ `app.js` - 添加云开发初始化
- ✅ `pages/index/index.js` - 改为调用云函数 `getVotes`
- ✅ `pages/create/create.js` - 改为调用云函数 `createVote`
- ✅ `pages/detail/detail.js` - 调用云函数获取详情、提交投票

### 2. 云函数列表

| 云函数名 | 功能 | 位置 |
|----------|------|------|
| `getOpenId` | 获取用户openId | miniprogram/cloudfunctions/getOpenId |
| `getVotes` | 获取投票列表 | miniprogram/cloudfunctions/getVotes |
| `getVoteDetail` | 获取投票详情 | miniprogram/cloudfunctions/getVoteDetail |
| `createVote` | 创建投票 | miniprogram/cloudfunctions/createVote |
| `submitVote` | 提交投票 | miniprogram/cloudfunctions/submitVote |
| `deleteVote` | 删除投票 | miniprogram/cloudfunctions/deleteVote |
| `checkVoteRecord` | 检查投票记录 | miniprogram/cloudfunctions/checkVoteRecord |

---

## 你需要完成的操作

### 第一步：修改云环境ID

编辑 `miniprogram/app.js`，将第7行的云环境ID替换为你的实际ID：

```javascript
wx.cloud.init({
  env: '你的云环境ID',  // 例如：campus-vote-xxxxx
  traceUser: true,
})
```

### 第二步：在微信开发者工具中操作

#### 2.1 开通云开发

1. 打开微信开发者工具
2. 导入项目 `C:\Users\Administrator\Desktop\微信小程序\campus-vote-app`
3. 点击顶部菜单「云开发」
4. 按照提示开通云开发
5. 复制云环境ID，填入 `app.js`

#### 2.2 创建数据库集合

登录 [微信云开发控制台](https://cloud.weixin.qq.com/)，在「数据库」中创建以下集合：

---

**集合1: `votes`**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| title | string | 投票标题 |
| description | string | 投票描述 |
| options | array | 选项数组 [{text: string, votes: number}] |
| voteType | string | single(单选) / multiple(多选) |
| endDate | string/null | 结束日期 |
| isAnonymous | boolean | 是否匿名 |
| creatorName | string | 创建者名称 |
| creatorOpenId | string | 创建者openid |
| createTime | date | 创建时间 |
| totalVoters | number | 总投票人数 |
| status | string | active(进行中) / ended(已结束) |

**集合权限设置（重要）：**

```json
{
  "read": true,
  "write": "doc._openid == auth.openid"
}
```

- ✅ 所有用户可读取投票列表
- ✅ 仅创建者可修改/删除自己创建的投票

**创建索引：**

| 索引名称 | 字段路径 | 排序 |
|----------|----------|------|
| idx_createTime | createTime | desc |

---

**集合2: `vote_records`**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| voteId | string | 投票ID |
| userOpenId | string | 用户openid |
| selectedOptions | array | 选择的选项索引 |
| voteTime | date | 投票时间 |

**集合权限设置（重要）：**

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

- ✅ 用户只能读写自己的投票记录
- ✅ 防止重复投票

**创建索引：**

| 索引名称 | 字段路径 | 排序 |
|----------|----------|------|
| idx_vote_user | voteId, userOpenId | asc, asc |

---

#### 2.3 部署云函数

1. 在微信开发者工具中，右键点击 `miniprogram/cloudfunctions` 文件夹
2. 选择「上传并部署：云端安装依赖」
3. 等待部署完成（每个云函数都需要单独部署）

或者逐个部署：
1. 展开云函数文件夹
2. 右键点击每个云函数文件夹（如 `getOpenId`）
3. 选择「上传并部署」
4. 重复直到所有云函数部署完成

---

### 第三步：测试项目

1. 在微信开发者工具中点击「编译」
2. 测试创建投票功能
3. 测试投票功能
4. 检查云开发控制台中数据是否正确写入

---

## 常见问题

### Q: 云函数调用失败
A: 检查云函数是否已部署，云环境ID是否正确

### Q: 数据库权限错误
A: 确保已按照上述说明设置集合权限

### Q: 如何查看云函数日志
A: 在微信开发者工具中，右键点击云函数，选择「查看日志」

---

## 项目文件结构

```
campus-vote-app/
├── project.config.json          # 项目配置
├── project.private.config.json  # 私有配置
└── miniprogram/
    ├── app.js                   # 应用入口（云初始化）
    ├── app.json                 # 全局配置
    ├── app.wxss                 # 全局样式
    ├── cloudfunctions/           # 云函数目录
    │   ├── getOpenId/
    │   ├── getVotes/
    │   ├── getVoteDetail/
    │   ├── createVote/
    │   ├── submitVote/
    │   ├── deleteVote/
    │   └── checkVoteRecord/
    └── pages/
        ├── index/               # 首页（投票列表）
        ├── create/              # 创建投票页
        └── detail/              # 投票详情页
```
